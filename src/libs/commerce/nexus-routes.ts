/**
 * Marketplace Nexus path constants used by both the service URL builders
 * and server-only OG/SSR fetchers. Keep route strings here so libs-layer
 * readers never import `marketplaceApi`.
 */
export const NEXUS_STREAM_LISTINGS_ROUTE = 'v0/stream/listings';
